# FINO AI Server 워크플로우 상세 문서

## 전체 워크플로우 개요

```
┌────────────────────────────────────────────────────────────────────────┐
│                        API 요청 (POST /generate/manual)                 │
│                              location, date_range                       │
└────────────────────────────────┬───────────────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────────────┐
│               generate_report_workflow (Main Task)                      │
│                       celery_worker.py:217                              │
└────────────────────────────────┬───────────────────────────────────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          ▼                      ▼                      ▼
   ┌─────────────┐     ┌─────────────────┐    ┌─────────────────┐
   │ Neo4j 조회  │     │ Executive       │    │  병렬 카테고리   │
   │ 뉴스 데이터 │ →   │ Summary 생성    │ →  │     분석        │
   └─────────────┘     └─────────────────┘    └────────┬────────┘
                                                       │
          ┌────────────┬────────────┬────────────┬─────┴─────┐
          ▼            ▼            ▼            ▼           ▼
     ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐  ┌────────┐
     │ 경제   │   │ 사회   │   │ 문화   │   │ 정치   │  │ 환경   │
     │ 분석   │   │ 분석   │   │ 분석   │   │ 분석   │  │ 분석   │
     └────┬───┘   └────┬───┘   └────┬───┘   └────┬───┘  └────┬───┘
          │            │            │            │           │
          └────────────┴────────────┴────────────┴─────┬─────┘
                                                       ▼
                              ┌─────────────────────────────────────┐
                              │     assemble_final_report           │
                              │     최종 리포트 조립 + FINO 전송     │
                              └─────────────────────────────────────┘
```

---

## 1. Celery Task 구조

| Task 이름 | 역할 | 재시도 | 타임아웃 |
|-----------|------|--------|----------|
| `generate_report_workflow` | 전체 워크플로우 지휘 | 2회 (10분 간격) | soft 600s / hard 720s |
| `analyze_single_category` | 단일 카테고리 분석 | 3회 (지수 백오프) | soft 600s / hard 720s |
| `assemble_final_report` | 결과 조립 및 전송 | 2회 (5분 간격) | soft 600s / hard 720s |
| `dispatch_weekly_reports` | 주간 리포트 배치 | 3회 | - |
| `dispatch_monthly_reports` | 월간 리포트 배치 | 3회 | - |

---

## 2. 워크플로우 단계별 상세

### STEP 1: 뉴스 데이터 조회
**파일 위치:** `celery_worker.py:237`

```python
all_news_data = graph_service.get_all_news_by_location(location, start_date, end_date)
```

- Neo4j에서 해당 지역/기간의 모든 뉴스 조회
- 데이터 없으면 `SKIPPED` 상태로 종료

---

### STEP 2: Executive Summary 생성
**파일 위치:** `report_chain.py:219`

```
뉴스 데이터
    │
    ▼ _rank_news_by_trending()
    │   └─ Google API로 화제성 점수 측정 (병렬 10스레드)
    │   └─ 점수 높은 순 정렬
    ▼
Top 5 뉴스 선정
    │
    ▼ _expand_context_with_chroma()
    │   └─ ChromaDB에서 각 뉴스별 유사 문서 3개 검색
    │   └─ 벡터 임베딩 기반 의미론적 검색
    ▼
확장된 컨텍스트
    │
    ▼ LangChain + vLLM
    │   └─ 프롬프트: 헤드라인 브리핑 + 주요 동향
    │   └─ JSON 파싱 (최대 3회 재시도)
    ▼
ExecutiveSummary (Dict)
```

**프롬프트 구조:**
- `headline_briefing`: 2-3문장 (100-150자) - 핵심 이슈 요약
- `key_trends`: 3-5문단 (300-500자) - 상황/배경/전망

---

### STEP 3: 병렬 카테고리 분석
**파일 위치:** `celery_worker.py:262-285`

```python
# Celery group으로 병렬 실행
workflow = chain(
    group(*category_tasks),      # 모든 카테고리 동시 분석
    assemble_final_report.s(...) # 결과 조립
)
```

각 카테고리 분석 (`report_chain.py:342`):

```
카테고리별 뉴스
    │
    ▼ _rank_news_by_trending()
    │   └─ 화제성 순 정렬
    ▼
Top 3 뉴스 선정
    │
    ▼ _expand_context_with_chroma()
    │   └─ 관련 문서 검색
    ▼
확장된 컨텍스트
    │
    ▼ LangChain + vLLM
    │   └─ 분야별 특화 프롬프트
    │   └─ 현황 → 심층 분석 → 전망 구조
    ▼
CategoricalAnalysis (Dict)
```

---

### STEP 4: 최종 조립
**파일 위치:** `celery_worker.py:120`

```python
final_report_json = {
    "location": location,
    "report_type": report_type,
    "generated_at": datetime.now().isoformat(),
    "executive_summary": executive_summary,
    "categorical_analysis": successful_analyses,
    "metadata": {...}
}
```

- 실패한 카테고리가 하나라도 있으면 전체 재시도
- 성공 시 FINO 서버로 전송

---

## 3. 화제성 랭킹 시스템

**파일 위치:** `report_chain.py:22`

```
┌─────────────────────────────────────────────────────────┐
│               _rank_news_by_trending()                  │
└─────────────────────────┬───────────────────────────────┘
                          │
    ┌─────────────────────┼─────────────────────┐
    ▼                     ▼                     ▼
┌────────┐          ┌────────┐           ┌────────┐
│ 뉴스1  │          │ 뉴스2  │           │ 뉴스N  │
│ 제목   │          │ 제목   │           │ 제목   │
└───┬────┘          └───┬────┘           └───┬────┘
    │ ThreadPool        │                    │
    │ (10 workers)      │                    │
    ▼                   ▼                    ▼
┌────────────────────────────────────────────────────┐
│           Google Custom Search API                  │
│    검색 결과 수 = 화제성 점수                       │
└────────────────────────────────────────────────────┘
    │
    ▼ 점수 내림차순 정렬
┌────────────────────────────────────────────────────┐
│  [뉴스3: 1200점] [뉴스1: 800점] [뉴스2: 450점] ... │
└────────────────────────────────────────────────────┘
```

---

## 4. 컨텍스트 확장 (RAG)

**파일 위치:** `report_chain.py:75`

```
┌─────────────────────────────────────────────────────────┐
│            _expand_context_with_chroma()                │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Top N 뉴스 제목들                                       │
│   ["데이터센터 유치...", "관광객 증가...", ...]         │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼ SentenceTransformers 임베딩
┌─────────────────────────────────────────────────────────┐
│ Query Vectors: [[0.12, 0.34, ...], [0.56, 0.78, ...]]  │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼ ChromaDB 유사도 검색
┌─────────────────────────────────────────────────────────┐
│ 각 뉴스별 관련 문서 3개씩 검색                          │
│   - 원본 뉴스 자체는 제외 (self_excluded)               │
│   - 거리 기준 정렬                                      │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│ 확장된 컨텍스트 (예시)                                  │
│                                                         │
│ ### 주요 뉴스 #1: 데이터센터 유치 성공                  │
│   - 관련 뉴스: IT 기업 유치 현황 (내용: ...)            │
│   - 관련 뉴스: 일자리 창출 효과 (내용: ...)             │
│                                                         │
│ ### 주요 뉴스 #2: 관광객 증가 추세                      │
│   - 관련 뉴스: 숙박업 매출 상승 (내용: ...)             │
└─────────────────────────────────────────────────────────┘
```

---

## 5. 재시도 전략

```
실패 발생
    │
    ▼ 1차 재시도 (60초 후)
    │
    ▼ 2차 재시도 (120초 후) ← 지수 백오프
    │
    ▼ 3차 재시도 (240초 후)
    │
    ▼ 최대 300초까지
    │
    └─ 최종 실패 시 상위 Task로 예외 전파
```

**재시도 설정** (`celery_worker.py:64-72`):

```python
@celery_app.task(
    max_retries=3,
    default_retry_delay=60,
    autoretry_for=(Exception,),
    retry_backoff=True,        # 지수 백오프 활성화
    retry_backoff_max=300,     # 최대 5분
    retry_jitter=True          # 랜덤 지연 추가
)
```

---

## 6. 스케줄링 (Celery Beat)

| 스케줄 | Task | 실행 시점 |
|--------|------|----------|
| 주간 리포트 | `dispatch_weekly_reports` | 매주 월요일 04:00 |
| 월간 리포트 | `dispatch_monthly_reports` | 매월 1일 05:00 |

**날짜 계산:**
- 주간: 지난주 월요일 00:00 ~ 이번주 월요일 00:00
- 월간: 지난달 1일 00:00 ~ 이번달 1일 00:00

---

## 7. 최종 리포트 JSON 구조

```json
{
  "location": "춘천",
  "report_type": "weekly",
  "generated_at": "2025-01-12T04:30:00",
  "executive_summary": {
    "headline_briefing": "핵심 이슈 2-3문장...",
    "key_trends": "주요 동향 3-5문단..."
  },
  "categorical_analysis": [
    {
      "category": "경제",
      "analysis_text": "경제 분야 심층 분석..."
    },
    {
      "category": "사회",
      "analysis_text": "사회 분야 심층 분석..."
    }
  ],
  "metadata": {
    "total_categories": 5,
    "successful_categories": 5,
    "report_quality": "complete"
  }
}
```

---

## 8. 관련 파일 참조

| 파일 | 역할 |
|------|------|
| `app/celery_worker.py` | Celery Task 정의, 워크플로우 오케스트레이션 |
| `app/chains/report_chain.py` | LangChain 분석 파이프라인, RAG 구현 |
| `app/services/graph_service.py` | Neo4j 뉴스 데이터 조회 |
| `app/services/google_api_service.py` | 화제성 점수 측정 |
| `app/services/vectorstore_service.py` | ChromaDB 벡터 검색 |
| `app/api/v1/reports.py` | REST API 엔드포인트 |
